<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.camaro4life18</groupId>
  <artifactId>IceMaker</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>IceMaker</name>
  <description>IceMaker</description>
  
  <properties>
    <!-- DEPENDENCIES VERSIONS -->
    <pi4j.version>3.0.2</pi4j.version> <!-- Updated Pi4J to version 3.0.2 for Java 24 compatibility -->
    <ehcache.version>3.10.8</ehcache.version>
    <!-- Removed explicit log4j.version and slf4j.version as spring-boot-starter-log4j2 will manage them -->

    <!-- PLUGIN VERSIONS -->
    <maven.compiler.version>3.14.0</maven.compiler.version>
    <maven.clean.version>3.5.0</maven.clean.version>
    <maven.resources.version>3.3.1</maven.resources.version>
    <maven.surefire.version>3.5.3</maven.surefire.version>
    <maven.jar.version>3.4.2</maven.jar.version>
    <maven.install.version>3.1.2</maven.install.version>
    <maven.deploy.version>3.1.2</maven.deploy.version>
    <maven.site.version>3.12.1</maven.site.version>
    <maven.project.info.reports.version>3.6.2</maven.project.info.reports.version>
    <maven.assembly.version>3.7.1</maven.assembly.version>
    <spring-boot-maven-plugin.version>3.3.1</spring-boot-maven-plugin.version> <!-- Latest stable Spring Boot Maven Plugin -->

    <!-- JAVA COMPILER SETTINGS -->
    <maven.compiler.source>21</maven.compiler.source> <!-- Pi4J 3.x is built for Java 21+ -->
    <maven.compiler.target>21</maven.compiler.target> <!-- Pi4J 3.x is built for Java 21+ -->
  </properties>

  <build>
	  <plugins>
	    <plugin>
	      <groupId>org.apache.maven.plugins</groupId>
	      <artifactId>maven-compiler-plugin</artifactId>
	      <version>${maven.compiler.version}</version>	
	      <configuration>
	        <source>${maven.compiler.source}</source>
	        <target>${maven.compiler.target}</target>
	      </configuration>
	    </plugin>
        <plugin>
          <artifactId>maven-clean-plugin</artifactId>
          <version>${maven.clean.version}</version>
        </plugin>  
        <plugin> 
          <artifactId>maven-resources-plugin</artifactId>
          <version>${maven.resources.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven.surefire.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-jar-plugin</artifactId>
          <version>${maven.jar.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-install-plugin</artifactId>
          <version>${maven.install.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-deploy-plugin</artifactId>
          <version>${maven.deploy.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-site-plugin</artifactId>
          <version>${maven.site.version}</version>
        </plugin>
        <plugin>
          <artifactId>maven-project-info-reports-plugin</artifactId>
          <version>${maven.project.info.reports.version}</version>
        </plugin>
        <plugin>
			<groupId>org.apache.maven.plugins</groupId>
			<artifactId>maven-assembly-plugin</artifactId>
			<version>${maven.assembly.version}</version>
		</plugin>
        <plugin>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-maven-plugin</artifactId>
            <version>${spring-boot-maven-plugin.version}</version>
            <configuration>
                <mainClass>com.camaro4life18.icemaker.IceMaker</mainClass>
            </configuration>
            <executions>
                <execution>
                    <goals>
                        <goal>repackage</goal>
                    </goals>
                </execution>
            </executions>
        </plugin>
      </plugins>
  </build>

  <dependencies>
		<dependency>
		    <groupId>com.pi4j</groupId>
		    <artifactId>pi4j-core</artifactId>
		    <version>${pi4j.version}</version>
		</dependency>
		<dependency>
		    <groupId>com.pi4j</groupId>
		    <artifactId>pi4j-plugin-raspberrypi</artifactId>
		    <version>${pi4j.version}</version>
		</dependency>
		<dependency>
		    <groupId>com.pi4j</groupId>
		    <artifactId>pi4j-plugin-pigpio</artifactId>
		    <version>${pi4j.version}</version>
		</dependency>
		
		<dependency>
		    <groupId>org.ehcache</groupId>
		    <artifactId>ehcache</artifactId>
		    <version>${ehcache.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.glassfish.jaxb</groupId>
                    <artifactId>jaxb-runtime</artifactId>
                </exclusion>
            </exclusions>
		</dependency>
		
        <!-- Use Spring Boot's Log4j2 starter for complete and managed Log4j2 integration -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-log4j2</artifactId>
            <version>${spring-boot-maven-plugin.version}</version> <!-- Align with Spring Boot version -->
        </dependency>
	</dependencies>
</project>
